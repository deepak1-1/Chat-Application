const defaultState = {
    token: null,
    user: {},
}

const auth = (state = defaultState, actions) => {
    switch (actions.type) {
        default:
            return state
    }
}

export default auth;